import 'package:flutter/material.dart';

Widget icon1(IconData icon) {
  return Icon(
    icon,
    color: Colors.white,
    size: 18,
  );
}

Widget icon2(IconData icon) {
  return Icon(
    icon,
    color: Colors.grey,
    size: 30,
  );
}
